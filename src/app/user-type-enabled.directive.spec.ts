import { UserTypeEnabledDirective } from './user-type-enabled.directive';

describe('UserTypeEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTypeEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
